import React from 'react';
import HeaderSignupPresentation from '../presentation';

const HeaderSignupContainer = props => {
  return <HeaderSignupPresentation {...props} />;
};

export default HeaderSignupContainer;
